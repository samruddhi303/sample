 
# import-->
# The import system ¶ Python code in one module gains access to the code in another module by the process of importing it.
# The import statement is the most common way of invoking the import machinery, but it is not the only way.


from math import sqrt, pi    #By using from keyword
result = sqrt(9)*pi
print(result)